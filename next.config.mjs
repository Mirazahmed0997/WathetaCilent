/** @type {import('next').NextConfig} */
const nextConfig = {
    // images: {
    //     remotePatterns: [new URL('http://localhost:3000/**'), new URL('https://admin.watheta.com/**')],
    // },
};

export default nextConfig;
